#############################################
# Public OpenAI (leave blank if using Azure)
#############################################
OPENAI_API_KEY=

#############################################
# Azure OpenAI (Azure AI Foundry)
# Fill all 3 core vars (endpoint, key, deployment) to enable Azure path.
#############################################
# Endpoint format: https://<your-resource-name>.openai.azure.com
AZURE_OPENAI_ENDPOINT=
AZURE_OPENAI_API_KEY=
# Deployment name you created (NOT base model name if different)
AZURE_OPENAI_DEPLOYMENT=
# Optional; code defaults to 2024-06-01 if omitted
AZURE_OPENAI_API_VERSION=2024-06-01

# Notes:
# - If Azure vars are present they take precedence.
# - Keep this file checked into git; do NOT commit your real keys (.env.local is ignored).
